function input(name, month, baseSalary) {
    if (month == "January") {
        const ans = baseSalary * 1;
        console.log(`The salary of ${name} for the month ${month} is ${ans}USD`);
    }
    else if (month == "February") {
        const ans = baseSalary * 2;
        console.log(`The salary of ${name} for the month ${month} is ${ans}USD`);
    }
    else if (month == "March") {
        const ans = baseSalary * 3;
        console.log(`The salary of ${name} for the month ${month} is ${ans}USD`);
    }


    else if (month == "April") {
        const ans = baseSalary * 4;
        console.log(`The salary of ${name} for the month ${month} is ${ans}USD`);
    }
    else if (month == "May") {
        const ans = baseSalary * 5;
        console.log(`The salary of ${name} for the month ${month} is ${ans}USD`);
    }
    else if (month == "June") {
        const ans = baseSalary * 6;
        console.log(`The salary of ${name} for the month ${month} is ${ans}USD`);
    }


    else if (month == "July") {
        const ans = baseSalary * 7;
        console.log(`The salary of ${name} for the month ${month} is ${ans}USD`);
    }
    else if (month == "August") {
        const ans = baseSalary * 8;
        console.log(`The salary of ${name} for the month ${month} is ${ans}USD`);
    }
    else if (month == "September") {
        const ans = baseSalary * 9;
        console.log(`The salary of ${name} for the month ${month} is ${ans}USD`);
    }

    else if (month == "October") {
        const ans = baseSalary * 10;
        console.log(`The salary of ${name} for the month ${month} is ${ans}USD`);
    }
    else if (month == "November") {
        const ans = baseSalary * 11;
        console.log(`The salary of ${name} for the month ${month} is ${ans}USD`);
    }else if (month == "December") {
        const ans = baseSalary * 12;
        console.log(`The salary of ${name} for the month ${month} is ${ans}USD`);
    }

}

input("Shovon", "December", 2000);
